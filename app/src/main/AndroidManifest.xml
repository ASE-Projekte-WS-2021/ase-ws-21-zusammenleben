<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WGFinance">
        <activity
            android:name="View.After.ActivityPaymentOverview"
            android:exported="false" />
        <activity
            android:name="View.Before.JoinFlatActivity"
            android:exported="true" />
        <activity
            android:name="View.Before.CreateFlatActivity"
            android:exported="true" />
        <activity
            android:name="View.Before.AddFlatActivity"
            android:exported="true" />
        <activity
            android:name="View.After.ActivityOverview"
            android:exported="true" />
        <activity
            android:name="View.Before.RegistrationActivity"
            android:exported="true" />

        <activity
            android:name="View.After.ActivityBasketList"
            android:exported="true" />

        <activity
            android:name="View.After.ActivityShoppingList"
            android:exported="true" />

        <activity
            android:name="View.Before.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>